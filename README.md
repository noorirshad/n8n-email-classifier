# n8n-email-classifier
Automated email classification workflow built with n8n. It uses AI (Google Gemini or OpenAI) to categorize incoming Gmail messages into order or enquiry. Includes batching, delay handling, and structured JSON output for reliable processing and easy integration with downstream systems.
